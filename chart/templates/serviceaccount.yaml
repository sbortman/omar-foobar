{{- if eq (include "omar-foobar.serviceAccount.enabled" $) "true" -}}
{{- $annotations := merge .Values.serviceAccount.annotations $.Values.global.serviceAccount.annotations -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "omar-foobar.serviceAccount.name" . }}
  labels:
    {{- include "omar-foobar.labels" . | nindent 4 }}
  {{- with $annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
